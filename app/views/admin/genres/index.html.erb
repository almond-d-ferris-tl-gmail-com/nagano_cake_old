<h1>Admin::Genres#index</h1>
<p>Find me in app/views/admin/genres/index.html.erb</p>

<h1>ジャンル一覧・追加</h1>

<%= @admin_genres.each do |genre| %>

  <p>ジャンル名</p>
  <span><%= genre.name %></span>
  <%= f.submit '新規登録', class: "btn btn-success" %>

    <table class="table table-striped">

      <thead>
        <tr>
          <th></th>
          <th>ジャンル</th>
          <th></th>
          <th colspan="3"></th>
        </tr>
      </thead>

    <tbody>
      <tr>
        <td><%= genre.name %><%= f.submit '編集する', class: "btn btn-success" %></td>
      </tr>
    </tbody>
    </table>

<% end %>
